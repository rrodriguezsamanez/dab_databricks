# This is a Declarative Automation Bundle definition for bakehouse_analytics.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bakehouse_analytics
  uuid: 5b985e8a-2f27-4b1c-bd89-41a78a6bbf03

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405611074894560.0.azuredatabricks.net
    variables:
      catalog: dtbrsmartd01
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-7405609441602257.17.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/rommel.rodriguez07@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/rommel.rodriguez07@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dtbrsmartd01
      schema: prod
    permissions:
      - user_name: rommel.rodriguez07@outlook.com
        level: CAN_MANAGE
